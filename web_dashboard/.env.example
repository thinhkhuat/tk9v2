# Web Dashboard Environment Configuration
# Story 1.3: User and Session Database Schema

# ============================================================
# Supabase Configuration (Story 1.3)
# ============================================================

# Supabase Project URL (required for database operations)
SUPABASE_URL=https://your-project.supabase.co

# Supabase Service Role Key (required for server-side operations, bypasses RLS)
# Get this from: Supabase Dashboard → Settings → API → service_role key
SUPABASE_SERVICE_KEY=your_service_role_key_here

# Supabase Anonymous Key (for client-side operations with RLS)
# Get this from: Supabase Dashboard → Settings → API → anon key
SUPABASE_ANON_KEY=your_anon_key_here

# JWT Secret (for validating JWT tokens from Supabase Auth)
# Get this from: Supabase Dashboard → Settings → API → JWT Secret
JWT_SECRET=your_jwt_secret_here

# ============================================================
# Server Configuration
# ============================================================

# Server host (0.0.0.0 for all interfaces, localhost for local only)
HOST=0.0.0.0

# Server port
PORT=12656

# ============================================================
# CORS Configuration
# ============================================================

# Allowed origins for CORS (comma-separated, or * for all)
# Production example: http://localhost:5173,https://yourdomain.com,https://tk9.thinhkhuat.com
# Development default includes common local ports
CORS_ORIGINS=http://localhost:5173,http://localhost:5174,http://127.0.0.1:5173,http://127.0.0.1:5174,http://localhost:12656,http://127.0.0.1:12656

# ============================================================
# Research Configuration
# ============================================================

# Default research language
RESEARCH_LANGUAGE=vi

# Maximum concurrent research sessions
MAX_CONCURRENT_SESSIONS=5

# Session timeout in seconds
SESSION_TIMEOUT=3600

# Research subject validation (must match models.py ResearchRequest)
MIN_SUBJECT_LENGTH=3
MAX_SUBJECT_LENGTH=1000

# File generation wait timeout (seconds)
FILE_WAIT_TIMEOUT=30

# Session cleanup interval (seconds) - how often to clean up old sessions
SESSION_CLEANUP_INTERVAL=3600
